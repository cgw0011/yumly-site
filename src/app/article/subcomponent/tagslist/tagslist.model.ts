export class TagsList {
    img:string;
    text:string;
    link:string;

    constructor(img:string, text:string, link:string) {
        this.img = img;
        this.text = text;
        this.link = link;
    }
}